name: UsingMsbuild
on:
  workflow_dispatch:

jobs:
  UsingMsbuild:
    name: UsingMsbuildjob
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
    
      - name: Set up msbuild
        uses: microsoft/setup-msbuild@v1.1
  
      - name: Build app for release
        run: msbuild HiPIMS-OCL.vcxproj -t:rebuild -verbosity:diag -property:Configuration=Release -restore

      - name: Run exe
        run: |
          "Release\HiPIMS Simulator.exe"
        shell: cmd

      - name: Upload executable
        uses: actions/upload-artifact@v2
        with:
          name: windowsUpload
          path: |
            "Release\HiPIMS Simulator.exe"
    