name: Compile Hip

on:
  workflow_dispatch:

jobs:
  windows:
    name: run on windows
    runs-on: windows-latest

    steps:
    - name: Install MSVC
      uses: ilammy/msvc-dev-cmd@v1.4.1

    - name: Checkout repository
      uses: actions/checkout@v2

    - name: compile
      run: cl /I . src/gpudemo.cpp /Fe:project /std:c++latest

